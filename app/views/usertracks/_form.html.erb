<%= form_for(usertrack) do |f| %>
  <% if usertrack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(usertrack.errors.count, "error") %> prohibited this usertrack from being saved:</h2>

      <ul>
      <% usertrack.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :guid %>
    <%= f.text_field :guid %>
  </div>

  <div class="field">
    <%= f.label :url %>
    <%= f.text_field :url %>
  </div>

  <div class="field">
    <%= f.label :datetime %>
    <%= f.text_field :datetime %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
